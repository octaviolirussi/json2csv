# ---------------------------------------------------------------------------
# json2csv – Python dependency manifest
# Use inside a virtual environment:
#   python3.13 -m venv .venv313
#   source .venv313/bin/activate
#   pip install -r requirements.txt
#   pip install -e .
# ---------------------------------------------------------------------------

# --- Runtime (none beyond stdlib) ------------------------------------------
# json2csv has no third-party runtime dependencies.

# --- Build -----------------------------------------------------------------
setuptools>=75.8.0
wheel

# --- Formatting & linting --------------------------------------------------
black>=24.0.0
ruff>=0.4.0

# --- Type checking ---------------------------------------------------------
mypy>=1.10.0
pyright>=1.1.360

# --- Testing ---------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0
hypothesis>=6.100.0

# --- Security --------------------------------------------------------------
bandit[toml]>=1.7.9

# --- Documentation ---------------------------------------------------------
pdoc>=14.0.0
