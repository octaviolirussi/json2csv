# json2csv – Documentación

La documentación de la API se genera automáticamente con **pdoc** y se
publica en GitHub Pages con cada PR exitoso fusionado a `main`.

Para generar localmente:

```bash
pdoc --output-directory docs/site src/json2csv
```

El resultado se encuentra en `docs/site/`.
