"""Version and build metadata for json2csv.

This module is the single source of truth for the package version.
It is read by ``pyproject.toml`` via ``importlib.metadata`` at install
time and can be imported directly from Python code.
"""

__version__: str = "1.0.0"
__build__: str = "000"
